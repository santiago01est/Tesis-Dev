name: dev_tesis
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  bloc: ^8.1.2
  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  fluttertoast: ^8.2.4
  get_it: ^7.6.4
  firebase_auth: ^4.15.0
  cloud_firestore: ^4.13.3
  go_router: ^14.1.3
  google_fonts: ^6.2.1
  carousel_slider: ^4.2.1
  universal_html: ^2.0.8
  excel: ^4.0.0
  flame: ^1.11.0
  flame_tiled: ^1.18.0
  number_inc_dec: ^0.8.0
  mockito: ^5.0.17
  fake_cloud_firestore: ^2.5.1



dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/items/
    - assets/items/ejemplosImg/
    - assets/items/estudio/
    - assets/images/
    - assets/images/objetos_animados/
    - assets/images/objetos_animados/animales/
    - assets/images/objetos_animados/items/
    - assets/images/objetos_animados/jugador/
    - assets/images/mapa/
    - assets/images/mapa/decoracion-suelo/
    - assets/images/mapa/decoracion-suelo/unidad1-Campo/
    - assets/images/mapa/decoracion-suelo/unidad2-Templo/
    - assets/images/mapa/objetos-UpAndDown/
    - assets/images/mapa/suelo/
    - assets/images/mapa/suelo/unidad1-Campo/
    - assets/images/mapa/suelo/unidad2-Templo/
    - assets/friends/
    - assets/buttons/
    - assets/avatares/
    - assets/fondos/
    - assets/testauto/
    - assets/tiles/





